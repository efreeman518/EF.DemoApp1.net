# Package.Infrastructure.Common.Contracts

Abstractions and marker types shared across infrastructure packages.

Included:
- `ICommonContractsAssemblyMarker` for assembly discovery.

This readme prevents NU5017 (package with no content nor dependencies).